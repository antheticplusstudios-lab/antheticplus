import { createFileRoute } from "@tanstack/react-router";
import { Check, Search, X } from "lucide-react";
import { useState } from "react";
import { Button } from "@/components/ui/button";
import { Input } from "@/components/ui/input";
import { demoPayments } from "@/lib/automations";
export const Route=createFileRoute("/_authenticated/staff/payments")({head:()=>({meta:[{title:"Payment Verifier — AntheticPlus Studios"},{name:"description",content:"Review pending manual payment submissions."},{property:"og:title",content:"AntheticPlus Payment Verifier"},{property:"og:description",content:"Staff workspace for payment approvals."},{property:"og:type",content:"website"},{name:"twitter:card",content:"summary_large_image"}]}),component:Page});function Page(){const [status,setStatus]=useState<Record<string,string>>({});const [query,setQuery]=useState("");const rows=demoPayments.filter(x=>(x.client+x.transaction+x.automation).toLowerCase().includes(query.toLowerCase()));return <div className="page-enter"><p className="text-sm font-bold text-primary">Staff workspace</p><div className="mt-2 flex flex-wrap items-end justify-between gap-4"><div><h1 className="text-3xl font-extrabold">Payment Verifier</h1><p className="mt-2 text-muted-foreground">Review submitted transfers before activating plans.</p></div><span className="rounded-full bg-secondary px-3 py-1.5 text-sm font-bold text-primary">{rows.filter(x=>!status[x.id]).length} pending</span></div><div className="relative mt-8 max-w-md"><Search className="absolute left-4 top-3.5 h-4 w-4 text-muted-foreground"/><Input value={query} onChange={e=>setQuery(e.target.value)} placeholder="Search client or transaction" className="h-11 rounded-xl pl-11"/></div><div className="mt-5 grid gap-4">{rows.map(row=><article key={row.id} className="rounded-2xl border border-border bg-card p-5"><div className="grid gap-5 lg:grid-cols-[1fr_1fr_auto] lg:items-center"><div><p className="font-extrabold">{row.client}</p><p className="mt-1 text-sm text-muted-foreground">{row.automation} · {row.submitted}</p></div><div className="text-sm"><p className="font-bold">{row.transaction}</p><p className="mt-1 text-muted-foreground">{row.method} · {row.amount}</p></div>{status[row.id]?<span className="rounded-full bg-secondary px-4 py-2 text-sm font-bold text-primary">{status[row.id]}</span>:<div className="flex gap-2"><Button onClick={()=>setStatus({...status,[row.id]:"Approved"})}><Check/>Approve</Button><Button variant="outline" onClick={()=>setStatus({...status,[row.id]:"Rejected"})}><X/>Reject</Button></div>}</div></article>)}</div></div>}